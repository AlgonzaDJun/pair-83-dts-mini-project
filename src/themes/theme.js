import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#141414",
    },
    secondary: {
      main: "#B9090B",
    },
    background: {
      default: "#141414",
    },
  },
});

export default theme;
