import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#141414",
    },
    secondary: {
      main: "#141414",
    },
    background: {
      default: "#141414",
    },
    // white: { main: "#ffffff" },
  },
});

export default theme;
